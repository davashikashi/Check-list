# Service
apiVersion: v1
kind: Service
metadata:
  name: nestjs-service
spec:
  selector:
    app: nestjs
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
      nodePort: 30081
  type: NodePort